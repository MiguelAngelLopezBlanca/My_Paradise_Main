import 'dart:ui';

const primaryColor = Color(0xFFD57A58);
const secundarColor = Color(0xFFEDFF86);
const colorTextos = Color(0xFF3B5249);
const thirdColor = Color(0xFF90F1EF);
const fourthColor = Color(0xFFDCCDE8);

const colorCampos = Color(0xFF00F3F3F3);

const colorPrincipal = Color(0xFF00B8C7);
const colorBotones = Color(0xFFF9C4632);
const colorSubtitulos1 = Color(0xFFD57A58);
const colorSubtitulos2 = Color(0xFF152945);
const fondoTextField = Color(0xFFF3F3F3); // Blanco Hueso

// v1.0.9+4
const double masterPercentGenericIconsWidth =
    0.102; // En base a un tamaño original de width=40 en Android Pixel 3a (392.72 ancho de pantalla)
const double masterPercentScoreIconsWidth =
    0.0458; // En base a un tamaño original de width=18 en Android Pixel 3a (392.72 ancho de pantalla)
const double masterPercentServiceIconsWidth =
    0.125; // En base a un tamaño original de width=50 en Android Pixel 3a (392.72 ancho de pantalla, redondeado a 400)
const double masterPercentServiceIconsMediumWidth =
    0.188; // v1.1.0+5 En base a un tamaño original de width=75 en Android Pixel 3a (392.72 ancho de pantalla, redondeado a 400)
const double masterPercentComplementaryServiceIconsWidth =
    0.09; // En base a un tamaño original de width=30 en Android Pixel 3a (392.72 ancho de pantalla)
const double masterPercentSkyTemperaturaIconsWidth =
    0.0539; // En base a un tamaño original de height=50 y width=21.16, es decir de con, aproximadamente, un ratio de aspecto del 50%, en Android Pixel 3a (392.72 ancho de pantalla)
const double masterPercentSeaTemperaturaIconsWidth =
    0.1048; // En base a un tamaño original de height=50 y width=41.16, en Android Pixel 3a (392.72 ancho de pantalla)
const double masterPercentImageHeigh =
    0.1752; // En base a un tamaño original de height=133, en Android Pixel 3a (759.28 alto de pantalla)
const double masterPercentOrderTopBarHeigh =
    0.0529; // En base a un tamaño original de height=40, en Android Pixel 3a (759.28 alto de pantalla)
const double masterPercentLookForIconsWidth =
    0.04; // En base a un Android Pixel 3a (392.72 ancho de pantalla)
const double masterPercentPaddingsFilterButtons =
    0.0127; // En base a un Android Pixel 3a (392.72 ancho de pantalla)
const double masterPercentExpandedTopBarHeigh =
    0.175; // En base a un tamaño original de height=96, en Android Pixel 3a (759.28 alto de pantalla). En la versión v1.0.8+3 0.175, donde height=140 en el Pixel 3a
const double masterPercentHomeLogoHeigh =
    0.1185; // En base a un tamaño original de height=133, en Android Pixel 3a (759.28 alto de pantalla)
const double masterPercentHomeSloganHeigh =
    0.0395; // En base a un tamaño original de height=133, en Android Pixel 3a (759.28 alto de pantalla)
const double masterPercentOnlySearchTopBarHeigh =
    0.030; // v1.1.0+5 En base a un tamaño original de height=24, en Android Pixel 3a (759.28 alto de pantalla)

const int masterScreenWidth =
    400; // Proviene del ancho de pantalla obtenido mediante MediaQuery.of(context).size.width en un iPhone 11 Pro Max
//
